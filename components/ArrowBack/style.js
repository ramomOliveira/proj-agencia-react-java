import styled from 'styled-components';

export const Wrapper = styled.div`
  > span {
    font-size: 40px;
    color: white;

    :hover {
      color: var(--green);
      cursor: pointer;
    }
  }
`;
